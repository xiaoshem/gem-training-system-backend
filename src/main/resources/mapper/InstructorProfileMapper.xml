<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.alan.exam.mapper.InstructorProfileMapper">
    <sql id="instructorFields">
        p.id,
        p.user_id AS userId,
        u.user_name AS userName,
        u.real_name AS realName,
        u.organization,
        u.position,
        u.phone,
        p.professional_title AS professionalTitle,
        p.specialties,
        p.qualification_certificate AS qualificationCertificate,
        p.introduction,
        p.status,
        p.create_time AS createTime
    </sql>

    <select id="selectInstructorPage" resultType="cn.org.alan.exam.model.vo.training.InstructorProfileVO">
        SELECT <include refid="instructorFields"/>
        FROM t_instructor_profile p
        JOIN t_user u ON u.id = p.user_id AND u.is_deleted = 0
        <where>
            p.is_deleted = 0
            <if test="keyword != null and keyword != ''">
                AND (u.real_name LIKE CONCAT('%', #{keyword}, '%')
                     OR u.user_name LIKE CONCAT('%', #{keyword}, '%')
                     OR p.specialties LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND p.status = #{status}
            </if>
        </where>
        ORDER BY p.create_time DESC, p.id DESC
    </select>

    <select id="selectEnabledOptions" resultType="cn.org.alan.exam.model.vo.training.InstructorProfileVO">
        SELECT <include refid="instructorFields"/>
        FROM t_instructor_profile p
        JOIN t_user u ON u.id = p.user_id AND u.is_deleted = 0 AND u.status = 1
        WHERE p.is_deleted = 0 AND p.status = 'ENABLED'
        ORDER BY u.real_name, p.id
    </select>

    <select id="selectTeacherCandidates" resultType="cn.org.alan.exam.model.vo.training.InstructorProfileVO">
        SELECT NULL AS id, u.id AS userId, u.user_name AS userName, u.real_name AS realName,
               u.organization, u.position, u.phone,
               CASE WHEN p.id IS NULL THEN 'UNCREATED' ELSE p.status END AS status
        FROM t_user u
        LEFT JOIN t_instructor_profile p ON p.user_id = u.id AND p.is_deleted = 0
        WHERE u.is_deleted = 0 AND u.status = 1 AND u.role_id = 2
        ORDER BY u.real_name, u.id
    </select>
</mapper>
